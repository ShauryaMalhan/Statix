---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: finops-gateway-ingress
  namespace: finops-system
  labels:
    app: finops-gateway
  annotations:
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTPS":443}]'
    alb.ingress.kubernetes.io/certificate-arn: arn:aws:acm:us-east-1:123456789012:certificate/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
spec:
  ingressClassName: alb
  rules:
    - host: ingest.your-startup.com
      http:
        paths:
          - path: /ingest
            pathType: Prefix
            backend:
              service:
                name: finops-gateway-svc
                port:
                  number: 3000
