---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: finops-agent-sa
  namespace: finops-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: finops-agent
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: finops-agent
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: finops-agent
subjects:
  - kind: ServiceAccount
    name: finops-agent-sa
    namespace: finops-system
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: finops-agent
  namespace: finops-system
  labels:
    app: finops-agent
spec:
  selector:
    matchLabels:
      app: finops-agent
  template:
    metadata:
      labels:
        app: finops-agent
    spec:
      terminationGracePeriodSeconds: 30
      hostPID: true
      serviceAccountName: finops-agent-sa
      tolerations:
        - operator: Exists
      containers:
        - name: finops-agent
          image: finops-agent@sha256:f1e2d3c4b5a6978012345678901234567890fedcba1234567890fedcba123456
          imagePullPolicy: Always
          securityContext:
            privileged: true
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sleep", "5"]
          env:
            - name: FINOPS_INGEST_URL
              value: http://finops-gateway-svc.finops-system.svc.cluster.local:3000/ingest
            - name: FINOPS_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: finops-secrets
                  key: api-token
            - name: FINOPS_NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: FINOPS_BPF_DIR
              value: /app/bpf
            - name: RUST_LOG
              value: info
          volumeMounts:
            - name: cgroup
              mountPath: /sys/fs/cgroup
              readOnly: true
            - name: proc
              mountPath: /proc
              readOnly: true
          ports:
            - name: metrics
              containerPort: 9091
              protocol: TCP
      volumes:
        - name: cgroup
          hostPath:
            path: /sys/fs/cgroup
            type: Directory
        - name: proc
          hostPath:
            path: /proc
            type: Directory
