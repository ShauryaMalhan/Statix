# Local development secrets — copy to `.env` (never commit `.env`).
# After cloning: cp .env.example .env && edit passwords.

CLICKHOUSE_PASSWORD=changeme_local_dev_only

# Optional — uncomment in docker-compose / agent when testing auth
# STATIX_API_TOKEN=changeme_api_token
