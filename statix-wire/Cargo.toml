[package]
name = "statix-wire"
version = "1.0.8"
edition = "2021"
description = "Shared ingest wire types (batch envelope, workload rows, flat rows for gateway coalescer)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
