[workspace]
# Only the host-target crates live in the workspace.
# finops-ebpf is excluded because it compiles to bpfel-unknown-none (BPF bytecode),
# a completely different target than the host machine. Including it here would cause
# cargo to try building it for x86_64, which would fail.
members = [
    "finops-common",
    "finops-user",
<<<<<<< HEAD
=======
    "finops-api",
>>>>>>> 57e6b31 (Fixed merge conflict and added boiler for phase 3)
]
resolver = "2"
